<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">    
  <Target Name="DotGitVersion" BeforeTargets="Build" >
    <Message Text="Janda.Dot.GitVersion -> $(GitVersion_SemVer)" Importance="High" />
  </Target>
</Project>