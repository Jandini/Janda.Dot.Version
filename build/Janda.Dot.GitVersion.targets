<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">    
  <Target Name="DotGitVersion" BeforeTargets="BeforeBuild" >
     <None Include="$(MSBuildThisFileDirectory)build\GitVersion.yml">
  </Target>
</Project>